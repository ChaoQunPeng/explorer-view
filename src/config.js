const CONFIG = {
  // 服务端接口地址（一般不用改）
  apiHost: 'http://localhost:3030/',
  // 资源服务器地址（我自己是用IIS部署的一个）
  resourceHost: 'http://localhost:8020/',
  // 本地工作根路径，用于读取本地文件
  diskRoot: 'D:/',
}

export {
  CONFIG
}